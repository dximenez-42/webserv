#ifndef SERVER_HPP
#define SERVER_HPP

#include "../../includes.hpp"

class Server
{
	private:
	public:
		Server();
		Server(const Server &other);
		Server &operator=(const Server &other);
		~Server();
};


#endif